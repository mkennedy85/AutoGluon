# -----------------------------
# Git LFS tracking rules
# -----------------------------
# Media demos (videos)
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.webm filter=lfs diff=lfs merge=lfs -text
# If you keep QuickTime sources too, uncomment:
# *.mov filter=lfs diff=lfs merge=lfs -text

# Model / binary artifacts
*.pkl filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text

# Optional: large archives / datasets (enable if you plan to version them)
# *.zip filter=lfs diff=lfs merge=lfs -text
# *.tar filter=lfs diff=lfs merge=lfs -text
# *.tar.gz filter=lfs diff=lfs merge=lfs -text
# *.7z filter=lfs diff=lfs merge=lfs -text
# *.parquet filter=lfs diff=lfs merge=lfs -text

# NOTE:
# - Do NOT LFS notebooks or plain text (Markdown/CSV) by default.
# - CSVs are diffable and often small; only LFS them if you know they are huge.
